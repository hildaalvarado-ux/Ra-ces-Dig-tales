name: raices_digitalesv1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  drift: ^2.20.0
  sqlite3_flutter_libs: ^0.5.26
  path_provider: ^2.1.4
  path: ^1.9.0

  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  image_picker: ^1.2.1
  file_picker: ^10.3.10
  share_plus: ^12.0.1
  archive: ^4.0.9
  cross_file: ^0.3.5+2
  uuid: ^4.5.3
  table_calendar: ^3.2.0
  flutter_local_notifications: ^20.1.0
  timezone: ^0.10.1
  intl: ^0.20.2
  provider: ^6.1.5+1
  permission_handler: ^11.3.1
  device_info_plus: ^11.1.0
  url_launcher: ^6.3.2
  crypto: ^3.0.6
  shared_preferences: ^2.3.2
  

dev_dependencies:
  drift_dev: ^2.20.0
  build_runner: ^2.4.12
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  assets:
    - assets/images/
    - assets/data/cultivos.json
    - assets/data/fertilizantes.json
    - assets/data/plagas.json
    - assets/data/pesticidas.json
    - assets/data/enfermedades.json
    - assets/cultivos/
    - assets/fertilizantes/
    - assets/plagas/
    - assets/plagas/referencia/
    - assets/pesticidas/
    - assets/enfermedades/
  

flutter_launcher_icons:
  android: true
  ios: true
  web:
    generate: true
    image_path: "assets/images/logo.png"
  windows:
    generate: true
    image_path: "assets/images/logo.png"
  macos:
    generate: true
    image_path: "assets/images/logo.png"
  linux:
    generate: true
    image_path: "assets/images/logo.png"
  image_path: "assets/images/logo.png"
  min_sdk_android: 21